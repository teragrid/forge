# Section 03 — High-Level Architecture

> **Template**: DAB Full
> Describe the system at the "boxes and arrows" level — components, boundaries, and primary flows.

---

## 3.1 Architecture Style

> Choose one or describe a hybrid:
> - [ ] Monolith
> - [ ] Modular monolith
> - [ ] Microservices
> - [ ] Event-driven
> - [ ] Serverless
> - [ ] Hexagonal (Ports & Adapters)
> - [ ] CQRS / Event Sourcing
> - [ ] Other: _TODO_

**Rationale**: _TODO — why this style for this feature?_

---

## 3.2 Component Topology

```
┌─────────────────────────────────────────────────────────────┐
│  Clients                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────────┐  │
│  │  Web App │  │ Mobile   │  │  Third-party / Partner   │  │
│  └────┬─────┘  └────┬─────┘  └───────────┬──────────────┘  │
└───────┼─────────────┼────────────────────┼─────────────────┘
        │             │                    │
        └─────────────▼────────────────────┘
                      │
              ┌───────▼──────────┐
              │   API Gateway    │  ← auth, rate-limit, routing
              └───────┬──────────┘
                      │
          ┌───────────┼───────────┐
          │           │           │
   ┌──────▼───┐ ┌─────▼────┐ ┌───▼──────┐
   │ Service A│ │ Service B│ │ Service C│
   └──────┬───┘ └─────┬────┘ └───┬──────┘
          │           │          │
   ┌──────▼───────────▼──────────▼───────┐
   │           Message Bus / Events       │
   └──────────────────────────────────────┘
```

> TODO: Replace with actual component diagram.

---

## 3.3 Component Responsibilities

| Component | Responsibility | Technology | Owned by |
|-----------|---------------|------------|----------|
| API Gateway | Auth enforcement, routing, rate-limit | TODO | Platform |
| Service A | TODO | TODO | Team A |
| Service B | TODO | TODO | Team B |
| Message Bus | Async event delivery | TODO | Platform |

---

## 3.4 External Integrations

| System | Direction | Protocol | Auth | Notes |
|--------|-----------|----------|------|-------|
| TODO | Inbound | HTTPS/REST | OAuth2 | |
| TODO | Outbound | gRPC | mTLS | |

---

## 3.5 API Contract Summary

> The full API contract is defined in `openapi.yaml` (auto-generated by `forge ship arch` with KB injection).
> `forge ship arch` uses `InvokeWithKnowledge` — the top-5 relevant KB entries are injected into
> the LLM prompt so the generated contract reflects project conventions automatically.
> Edit `openapi.yaml` for authoritative contracts; the table below is a summary.
>
> **API style — choose one and remove the other rows:**

| Method | Path | Style | Summary | Auth Required |
|--------|------|-------|---------|---------------|
| POST | /api/v1/TODO | REST | TODO | Bearer JWT |
| GET | /api/v1/TODO/{id} | REST | TODO | Bearer JWT |
| POST | /rest/v1/rpc/TODO | Supabase RPC | TODO (delete if not using Supabase) | Supabase anon key |

> `detectAPIStyle()` in `forge ship` reads this file: if `/rest/v1/rpc/` paths are present,
> downstream `test`, `breakdown`, and `code` checkpoints automatically inject Supabase-specific guidance.

See [openapi.yaml](../../../.forge/specs/TODO/openapi.yaml) for the full OpenAPI 3.1.0 specification.

---

## 3.6 Key Design Decisions

| Decision | Options Considered | Chosen | Rationale |
|----------|--------------------|--------|-----------|
| TODO | A vs B | A | TODO |

---

*Next section: [04-data-design.md](04-data-design.md)*
