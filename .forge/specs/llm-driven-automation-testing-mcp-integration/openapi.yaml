openapi: "3.1.0"
info:
  title: "llm-driven automation testing mcp integration"
  version: "0.0.0-draft"
  description: |
    API contract for llm-driven automation testing mcp integration.
    Edit this file, then re-run `forge ship arch` to validate.
    API style: replace /api/v1/TODO with /rest/v1/rpc/{fn} if using Supabase RPC.
paths:
  /api/v1/TODO:
    post:
      summary: "TODO: describe the primary operation"
      operationId: todoCreate
      tags:
        - TODO
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TODORequest'
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TODOResponse'
        "400":
          description: Invalid request payload
        "401":
          description: Authentication required
        "422":
          description: Unprocessable entity
        "500":
          description: Internal server error
components:
  schemas:
    TODORequest:
      type: object
      required: []
      properties: {}
    TODOResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
