// vitest.config.ts — {{.Name}}
// Generated by forge new ts-service (forge {{.ForgeVer}})

import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    // Run each test file in an isolated environment
    environment: 'node',
    // Include coverage from all source files, not just tested ones
    coverage: {
      provider: 'v8',
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.stubs.ts', 'src/**/*.types.ts', 'src/main.ts'],
      // Minimum thresholds — tighten as coverage grows
      thresholds: {
        statements: 60,
        branches: 60,
        functions: 60,
        lines: 60,
      },
    },
    // Match both co-located tests and a top-level tests/ dir
    include: ['src/**/*.test.ts', 'tests/**/*.test.ts'],
    // Fail the suite if any test exceeds 10 s (network / DB tests should use testcontainers)
    testTimeout: 10_000,
    // Always run in parallel; individual tests can use `test.sequential` if needed
    pool: 'threads',
  },
});
