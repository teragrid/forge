// forge.config.ts — {{.Name}}
// Generated by forge new ts-service (forge {{.ForgeVer}})
//
// Typed project configuration consumed by the Forge CLI.
// Run `forge doctor` to validate against the current config schema.
// Full schema: https://forgeframework.dev/docs/config

/** @type {import('@forge/framework').ForgeConfig} */
const config = {
  project: '{{.Name}}',
  version: '0.0.1',
  runtime: 'node',
  description: '{{.Description}}',

  // ── Tenancy ──────────────────────────────────────────────────────────────
  tenancy: {
    // model: 'workspace' | 'account' | 'none'
    model: 'workspace',
    // isolationLevel: 'row' (RLS) | 'schema' | 'database'
    isolationLevel: 'row',
    tenantColumn: 'workspace_id',
  },

  // ── Authentication ────────────────────────────────────────────────────────
  auth: {
    // provider: 'supabase' | 'auth0' | 'custom'
    provider: 'custom',
    sessionStrategy: 'jwt',
    jwtExpiry: '15m',
    refreshExpiry: '7d',
  },

  // ── Database ──────────────────────────────────────────────────────────────
  database: {
    provider: 'postgres',
    migrations: './migrations',
    // Connection string from environment — never hardcode here
    // url: process.env.DATABASE_URL
  },

  // ── Security ──────────────────────────────────────────────────────────────
  security: {
    owasp: 'strict',
    rateLimit: true,
    auditLog: true,
    secretScan: true,
    webhookSignatureRequired: true,
  },

  // ── Testing ───────────────────────────────────────────────────────────────
  testing: {
    coverage: { statements: 80, branches: 70 },
    rlsTests: true,
    schemaAlignment: true,
  },

  // ── LLM Integration ───────────────────────────────────────────────────────
  llm: {
    instructionsFormat: 'all',
    contextBudget: 32000,
    instructionsDir: '.forge/instructions',
  },

  // ── Telemetry ─────────────────────────────────────────────────────────────
  telemetry: {
    enabled: true,
    provider: 'opentelemetry',
    // endpoint: process.env.OTEL_EXPORTER_OTLP_ENDPOINT
  },
} as const;

export default config;
