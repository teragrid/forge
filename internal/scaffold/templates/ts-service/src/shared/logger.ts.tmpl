// src/shared/logger.ts — {{.Name}}
// Generated by forge new ts-service (forge {{.ForgeVer}})
//
// Thin pino wrapper. Import this instead of pino directly so you can swap
// the underlying library without changing import paths across the codebase.

import pino from 'pino';

const isDev = process.env.NODE_ENV !== 'production';

export const logger = pino({
  level: process.env.LOG_LEVEL ?? (isDev ? 'debug' : 'info'),
  // Pretty-print in development; structured JSON in production
  transport: isDev
    ? { target: 'pino-pretty', options: { colorize: true } }
    : undefined,
  base: {
    service: '{{.Name}}',
    env: process.env.NODE_ENV ?? 'development',
  },
  redact: {
    // Never log secrets, tokens, or password hashes
    paths: ['*.password', '*.passwordHash', '*.token', '*.secret', '*.apiKey'],
    censor: '[REDACTED]',
  },
});
