// app/api/health/route.ts — {{.Name}}
// Generated by forge new next-app (forge {{.ForgeVer}})
//
// /healthz — liveness probe (no external deps checked)
// /api/health — readiness probe (returned as JSON)

import { NextResponse } from 'next/server'

export const runtime = 'nodejs'

export function GET() {
  return NextResponse.json(
    {
      status: 'ok',
      project: '{{.Name}}',
      timestamp: new Date().toISOString(),
    },
    { status: 200 },
  )
}
