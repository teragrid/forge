// forge.config.ts — {{.Name}}
// Generated by forge new next-app (forge {{.ForgeVer}})

/** @type {import('@forge/framework').ForgeConfig} */
const config = {
  project: '{{.Name}}',
  version: '0.0.1',
  runtime: 'nextjs',
  description: '{{.Description}}',

  // ── Security ──────────────────────────────────────────────────────────────
  security: {
    owasp: 'strict',
    secretScan: true,
    auditLog: true,
  },

  // ── Testing ───────────────────────────────────────────────────────────────
  testing: {
    coverage: { statements: 80, branches: 70 },
    e2eTests: true,
  },

  // ── LLM Integration ───────────────────────────────────────────────────────
  llm: {
    instructionsFormat: 'all',
    contextBudget: 32000,
    instructionsDir: '.forge/instructions',
  },
}

export default config
